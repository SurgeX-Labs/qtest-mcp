apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: qtest-mcp-route
spec:
  to:
    kind: Service
    name: qtest-mcp-service
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
